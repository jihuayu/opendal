[package]
name = "async-io"

edition = "2021"
version = "0.1.0"

[dependencies]
futures = "0.3"
opendal = {path = "../../../core", features = ["services-surreal"]}
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
